const header=document.querySelector('[data-header]');const menuButton=document.querySelector('[data-menu-button]');const nav=document.querySelector('[data-nav]');const form=document.querySelector('[data-contact-form]');
const updateHeader=()=>header.classList.toggle('scrolled',window.scrollY>16);updateHeader();window.addEventListener('scroll',updateHeader,{passive:true});
menuButton?.addEventListener('click',()=>{const open=menuButton.getAttribute('aria-expanded')==='true';menuButton.setAttribute('aria-expanded',String(!open));nav.classList.toggle('open',!open);document.body.classList.toggle('menu-open',!open)});
nav?.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{menuButton?.setAttribute('aria-expanded','false');nav.classList.remove('open');document.body.classList.remove('menu-open')}));
document.querySelector('[data-year]').textContent=new Date().getFullYear();
const showError=(field,message)=>{field.setAttribute('aria-invalid',message?'true':'false');const error=document.getElementById(`${field.id}-error`);if(error)error.textContent=message};
form?.addEventListener('submit',event=>{event.preventDefault();const data=new FormData(form);const fields={name:form.elements.name,email:form.elements.email,project:form.elements.project,message:form.elements.message};let valid=true;Object.values(fields).forEach(field=>showError(field,''));if(!data.get('name').trim()){showError(fields.name,'Please enter your name.');valid=false}if(!/^\S+@\S+\.\S+$/.test(data.get('email').trim())){showError(fields.email,'Please enter a valid email address.');valid=false}if(!data.get('project')){showError(fields.project,'Please choose an option.');valid=false}if(!data.get('message').trim()){showError(fields.message,'Please share a short description.');valid=false}if(!valid){form.querySelector('[aria-invalid="true"]')?.focus();return}const subject=encodeURIComponent(`Website enquiry: ${data.get('project')}`);const body=encodeURIComponent(`Name: ${data.get('name')}\nEmail: ${data.get('email')}\nCompany: ${data.get('company')||'Not provided'}\nInterest: ${data.get('project')}\n\nProject need:\n${data.get('message')}`);window.location.href=`mailto:kkuhlekuyeza23@gmail.com?subject=${subject}&body=${body}`;const note=form.querySelector('[data-form-note]');note.textContent='Your email app should now be open with the enquiry ready to send.'});
